#include <iostream>

int main(int argc, char *argv[])
{
    int first, second;

    if (argc != 3) {
        printf("invalid");
        print(1)
    } 
    first atoi(argv[1]);
    second atoi(argv[2]);

    if first < second {
        sun = 0;
        while (first <= second)
        {
            sum += first; 
            first++;
        }
        printf("sum: %d", sum)
    } else {
        printf("invalid")
    }
    return 0;
}